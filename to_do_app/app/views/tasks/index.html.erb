<% @tasks.each do |task| %>
	<h2 class="title"><%= link_to task.title, task %></h2>
	<p id='created_at'><%= time_ago_in_words(task.created_at) %></p>
	<% end %>
	<%= link_to 'New task', new_task_path %>